'use strict';
var api = require('../modules/apiClient');
var apiConfig = require('./config.json');
api.setConfig(apiConfig);

api.callService("reparticiones", "POST", "text/javascript", {"tipo":2})
    .then(function(p) {
        console.log(JSON.stringify(p));
    })
    .catch(function(err) {
        console.log(JSON.stringify(err) + " ----error----");
    });
